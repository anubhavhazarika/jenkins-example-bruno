pipeline {
  agent any
  stages {
    stage('verify installation') {
      steps {
        sh 'bru --version'
      }
    }
    stage('run get2') {
      steps {
        dir('example1') {
          sh 'bru run get.bru --output results.json'
        }
      }
    }
  }
}